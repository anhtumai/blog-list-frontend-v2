import Blogs from "./Blogs";
import LoginBanner from "./LoginBanner";

const HomePage = () => {
  return (
    <div>
      <LoginBanner />
      <Blogs />
    </div>
  );
};

export default HomePage;
